# Mindoro Validation Archive Decision

## Decision Memo

This repo will preserve the full March-family Mindoro validation provenance without presenting every preserved lane as thesis-facing evidence. The March 13 -> March 14 R1 primary validation row will be the only thesis-facing Mindoro Phase 3B validation row used in the main paper. The March 13 -> March 14 R0 archived baseline, any already-generated older March13-14 outputs that included R0, and the preserved March-family legacy rows will be retained in the repository for archival provenance and reproducibility only. They will not be used in the thesis-facing methodology, tables, figures, or headline validation claims.

## Paste-Ready Thesis Wording

The March 13 -> March 14 R1 row will be the only thesis-facing Mindoro Phase 3B validation row used in the main paper. The March 13 -> March 14 R0 baseline, any already-generated older March13-14 outputs that included R0, and the preserved March-family legacy rows will be retained in the repository for archival provenance and reproducibility only. They will not be used in the thesis-facing methodology, tables, figures, or headline validation claims.

## Archive Page Rule

The `Mindoro Validation Archive` page is a provenance-only and audit-oriented surface built from repo-preserved outputs. It is not thesis-facing evidence and should not be cited as the main Mindoro validation row. The main paper will use the March 13 -> March 14 R1 primary validation row only.

## Naming Note

In repo docs and UI labels, `March 13 -> March 14 R1` and `March 13 -> March 14 R0` refer to Phase 3B validation branches. They are not the same labels as the Phase 1 Recipe Code family (`R1`, `R2`, `R3`, `R4`). Use explicit phrasing such as `March 13 -> March 14 R1 primary validation row` and `March 13 -> March 14 R0 archived baseline` whenever ambiguity is possible.

## Provenance Guardrail

This decision is a presentation and reporting-scope decision. It does not delete stored outputs, does not claim undocumented scientific invalidity, and does not rewrite the frozen March 3 -> March 6 base-case provenance.
